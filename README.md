# Hashavat Aveda Server Side

you can clone and apply packages and build the project

you need to restore the 2 DBs under the `sql` folder

in `program.cs` update your email and email passord @line 162